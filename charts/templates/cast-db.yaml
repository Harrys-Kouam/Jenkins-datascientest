{{- if .Values.castDb.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cast-db-pvc
  labels:
    app: cast-db
    {{- include "fastapiapp.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.castDb.persistence.size }}
  {{- if .Values.castDb.persistence.storageClass }}
  storageClassName: {{ .Values.castDb.persistence.storageClass }}
  {{- end }}
---
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: cast-db-secret
  labels:
    app: cast-db
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.castDb.auth.username | quote }}
  POSTGRES_PASSWORD: {{ .Values.castDb.auth.password | quote }}
  POSTGRES_DB: {{ .Values.castDb.auth.database | quote }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cast-db
  labels:
    app: cast-db
    {{- include "fastapiapp.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: cast-db
  template:
    metadata:
      labels:
        app: cast-db
    spec:
      containers:
        - name: postgres
          image: "{{ .Values.castDb.image.repository }}:{{ .Values.castDb.image.tag }}"
          imagePullPolicy: {{ .Values.castDb.image.pullPolicy }}
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          envFrom:
            - secretRef:
                name: cast-db-secret
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            {{- toYaml .Values.castDb.resources | nindent 12 }}
      volumes:
        - name: data
          {{- if .Values.castDb.persistence.enabled }}
          persistentVolumeClaim:
            claimName: cast-db-pvc
          {{- else }}
          emptyDir: {}
          {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: cast-db
  labels:
    app: cast-db
    {{- include "fastapiapp.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: cast-db
  ports:
    - port: {{ .Values.castDb.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgres
